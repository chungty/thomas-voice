import { readFile, writeFile, mkdir, rm, copyFile } from 'node:fs/promises';
import { createHash } from 'node:crypto';

const root = new URL('../', import.meta.url);
const src = new URL('../src/', import.meta.url);
const dist = new URL('../dist/', import.meta.url);
const system = JSON.parse(await readFile(new URL('voice-system.json', src), 'utf8'));
const schema = JSON.parse(await readFile(new URL('schema.json', src), 'utf8'));

await rm(dist, { recursive: true, force: true });
await mkdir(new URL('machine/', dist), { recursive: true });
const voiceBytes = await readFile(new URL('voice-system.json', src));
const schemaBytes = await readFile(new URL('schema.json', src));
await writeFile(new URL('machine/voice-system.json', dist), voiceBytes);
await writeFile(new URL('machine/schema.json', dist), schemaBytes);

const esc = (s = '') => String(s).replace(/[&<>"']/g, (c) => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const slugLabel = (s) => s.replaceAll('-', ' ');

const css = `
:root{--paper:#f5f2e9;--ink:#171714;--muted:#68675f;--rule:#cbc7b9;--accent:#b84b2d;--accent-soft:#ead3c8;--code:#24231f;--max:82rem;--serif:Iowan Old Style,Palatino Linotype,Book Antiqua,Georgia,serif;--sans:Avenir Next,Avenir,Helvetica Neue,Arial,sans-serif;--mono:SFMono-Regular,Consolas,Liberation Mono,monospace}
*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:var(--paper);color:var(--ink);font-family:var(--sans);font-size:17px;line-height:1.62}.skip-link{position:absolute;left:-999px}.skip-link:focus{left:1rem;top:1rem;background:#fff;padding:.75rem;z-index:9}a{color:inherit;text-decoration-color:var(--accent);text-underline-offset:.2em}a:hover{color:var(--accent)}a:focus-visible,button:focus-visible{outline:3px solid var(--accent);outline-offset:3px}header{border-bottom:1px solid var(--rule);position:sticky;top:0;background:color-mix(in srgb,var(--paper) 94%,transparent);backdrop-filter:blur(12px);z-index:3}.nav{max-width:var(--max);margin:auto;padding:1rem 2rem;display:flex;align-items:baseline;gap:2rem}.brand{font-family:var(--serif);font-size:1.2rem;text-decoration:none;margin-right:auto}.nav a:not(.brand){font-size:.82rem;text-transform:uppercase;letter-spacing:.08em;text-decoration:none}.hero{max-width:var(--max);margin:auto;padding:7rem 2rem 5rem;display:grid;grid-template-columns:minmax(0,1.35fr) minmax(16rem,.65fr);gap:5rem;align-items:end}.eyebrow,.kicker{font:600 .75rem/1.3 var(--mono);text-transform:uppercase;letter-spacing:.1em;color:var(--accent)}h1,h2,h3{font-family:var(--serif);font-weight:500;line-height:1.04;letter-spacing:-.025em}h1{font-size:clamp(3.2rem,8vw,7.7rem);margin:.35rem 0 1.5rem;max-width:10ch}h2{font-size:clamp(2.2rem,5vw,4.3rem);margin:.2rem 0 2.2rem}h3{font-size:1.65rem;margin:.25rem 0 .65rem}.lede{font-family:var(--serif);font-size:clamp(1.25rem,2vw,1.7rem);line-height:1.35;max-width:39ch}.meta{border-top:1px solid var(--ink);padding-top:1rem}.meta p{margin:.2rem 0;color:var(--muted);font-size:.9rem}.band{border-top:1px solid var(--rule)}.section{max-width:var(--max);margin:auto;padding:5rem 2rem}.intro{display:grid;grid-template-columns:1fr 2fr;gap:4rem}.intro>p{max-width:62ch;margin-top:0}.grid{display:grid;grid-template-columns:repeat(12,1fr);gap:1.25rem}.card{grid-column:span 4;border-top:2px solid var(--ink);padding:1.2rem 0 2rem}.card p{margin:.45rem 0;color:var(--muted)}.card .rule{color:var(--ink)}.number{font:500 .8rem var(--mono);color:var(--accent)}.surface{grid-column:span 6;border-top:1px solid var(--ink);padding:1.5rem 0 2.5rem;display:grid;grid-template-columns:8rem 1fr;gap:1.5rem}.meters{display:flex;gap:3px;margin-top:1rem}.meter{width:18px;height:5px;background:var(--rule)}.meter.on{background:var(--accent)}.specimen{display:grid;grid-template-columns:1fr 1fr;gap:1px;background:var(--rule);border:1px solid var(--rule);margin:2rem 0}.sample{background:var(--paper);padding:2rem}.sample.negative{background:#eee9df;color:#5e554e}.sample blockquote{font-family:var(--serif);font-size:1.25rem;line-height:1.45;margin:1rem 0;white-space:pre-line}.annotation{font-size:.9rem;color:var(--muted)}.dark{background:var(--code);color:#f7f4ec}.dark .section{padding-top:6rem;padding-bottom:6rem}.dark .kicker,.dark a{color:#e99876}.dark .card{border-color:#817d70}.dark .card p{color:#bcb7aa}.steps{counter-reset:step;max-width:58rem}.step{counter-increment:step;display:grid;grid-template-columns:3rem 1fr;border-top:1px solid #777267;padding:1.25rem 0}.step:before{content:counter(step,decimal-leading-zero);font:500 .8rem var(--mono);color:#e99876}.code{font:400 .86rem/1.65 var(--mono);white-space:pre-wrap;background:#171714;color:#f5f2e9;padding:1.5rem;overflow:auto}.notice{border:1px solid var(--accent);padding:1.25rem;max-width:55rem}.test{display:grid;grid-template-columns:10rem 1fr 5rem;gap:1rem;border-top:1px solid var(--rule);padding:1rem 0}.pill{font:600 .7rem var(--mono);text-transform:uppercase;letter-spacing:.07em}.footer{border-top:1px solid var(--rule);padding:3rem 2rem}.footer>div{max-width:var(--max);margin:auto;display:flex;justify-content:space-between;gap:2rem}.desktop-nav{display:flex;gap:2rem}.mobile-nav{display:none}@media(max-width:760px){body{font-size:16px}.nav{padding:1rem 1.25rem;position:relative}.desktop-nav{display:none}.mobile-nav{display:block}.mobile-nav summary{font:600 .78rem var(--mono);text-transform:uppercase;letter-spacing:.08em;cursor:pointer}.mobile-nav div{position:absolute;top:100%;left:0;right:0;background:var(--paper);border-bottom:1px solid var(--rule);padding:1rem 1.25rem;display:grid;gap:1rem}.mobile-nav a{display:block!important;font-size:.9rem!important}.hero{grid-template-columns:1fr;gap:1.75rem;padding:3rem 1.25rem 2.75rem}h1{font-size:clamp(2.8rem,14vw,4.4rem);max-width:11ch}.section{padding:3.5rem 1.25rem}.intro{grid-template-columns:1fr;gap:1rem}.card,.surface{grid-column:1/-1}.surface{grid-template-columns:1fr}.specimen{grid-template-columns:1fr}.test{grid-template-columns:1fr;gap:.3rem}.footer>div{display:block}}@media (prefers-reduced-motion: reduce){html{scroll-behavior:auto}*{animation:none!important;transition:none!important}}
`;

const navLinks = `<a href="/foundations/">Foundations</a><a href="/components/">Components</a><a href="/surfaces/">Surfaces</a><a href="/specimens/">Specimens</a><a href="/tests/">Tests</a><a href="/for-agents/">For agents</a>`;
const nav = `<header><nav class="nav" aria-label="Primary"><a class="brand" href="/">Thomas Voice</a><div class="desktop-nav">${navLinks}</div><details class="mobile-nav"><summary>Menu</summary><div>${navLinks}</div></details></nav></header>`;
const footer = `<footer class="footer"><div><span>Thomas Voice · ${esc(system.content_version)}</span><span>A drafting system, not identity or authority</span></div></footer>`;
const shell = (title, body, description) => `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)} · Thomas Voice</title><meta name="description" content="${esc(description)}"><link rel="alternate" type="application/json" href="/machine/manifest.json"><style>${css}</style></head><body><a class="skip-link" href="#main">Skip to content</a>${nav}<main id="main">${body}</main>${footer}</body></html>`;

const hero = (eyebrow, title, lede, aside = '') => `<section class="hero"><div><p class="eyebrow">${esc(eyebrow)}</p><h1>${esc(title)}</h1><p class="lede">${esc(lede)}</p></div><div class="meta">${aside}</div></section>`;
const card = (item, i) => `<article class="card"><span class="number">${String(i + 1).padStart(2,'0')}</span><h3>${esc(item.name)}</h3><p class="rule">${esc(item.rule || item.use_when || item.job)}</p>${item.why ? `<p>${esc(item.why)}</p>` : ''}</article>`;

const home = hero('Public voice system', 'Draft for the surface. Keep the judgment.', 'A public system for agents writing with Thomas’s preferences: start with the job, stay exact about what is known, and change the composition to fit the audience and medium.', `<p><a href="/for-agents/">Agent quickstart →</a></p><p>Contract ${system.contract_version}</p><p>${system.surfaces.length} surfaces · ${system.components.length} components · ${system.tests.length} tests</p>`) +
`<section class="band"><div class="section intro"><div><p class="kicker">The model</p><h2>Voice is a system of decisions</h2></div><p>The same thought should not look the same in a quick chat, a sensitive note, and a public essay. What stays constant is the judgment: start with the job, name the concrete thing, match confidence to evidence, and connect systems to people. Surface rules change the composition.</p></div></section>
<section class="band"><div class="section"><p class="kicker">Foundations</p><h2>The parts that stay true</h2><div class="grid">${system.foundations.slice(0,6).map(card).join('')}</div><p><a href="/foundations/">Read all foundations →</a></p></div></section>
<section class="dark"><div class="section"><p class="kicker">For agents</p><h2>Load less. Resolve conflicts explicitly.</h2><div class="steps">${system.agent_protocol.steps.map(s=>`<div class="step"><div>${esc(s)}</div></div>`).join('')}</div><p><a href="/for-agents/">Open the agent contract →</a></p></div></section>
<section class="band"><div class="section"><p class="kicker">Surface matrix</p><h2>One voice, different jobs</h2><div class="grid">${system.surfaces.map((s,i)=>`<article class="surface"><div><span class="number">${String(i+1).padStart(2,'0')}</span><h3>${esc(s.name)}</h3></div><div><p>${esc(s.job)}</p><p class="annotation">Open: ${esc(s.defaults.opening)}<br>End: ${esc(s.defaults.ending)}</p><div class="meters" aria-label="Directness ${s.recipe.directness} out of 5">${[1,2,3,4,5].map(n=>`<span class="meter ${n<=s.recipe.directness?'on':''}"></span>`).join('')}</div></div></article>`).join('')}</div><p><a href="/surfaces/">Compare every surface →</a></p></div></section>
<section class="band"><div class="section"><p class="kicker">Boundary</p><h2>Drafting is not impersonation</h2><div class="notice">${esc(system.boundaries.notice)} Voice resemblance is never identity verification.</div></div></section>`;

const foundations = hero('Foundations', 'The decisions beneath the words', 'These rules survive a change in audience, channel, or length. Each one describes observable behavior and links to a test.', `<p>${system.foundations.length} foundations</p><p>Every rule test-linked</p>`) + `<section class="band"><div class="section"><div class="grid">${system.foundations.map(card).join('')}</div></div></section>`;
const components = hero('Components', 'Reusable moves, not canned phrases', 'Components define how to make a recommendation, report status, disagree, apologize, explain, or ask. They give an agent structure without giving it lines to copy.', `<p>${system.components.length} components</p><p>Rhetorical, not lexical</p>`) + `<section class="band"><div class="section"><div class="grid">${system.components.map((c,i)=>`<article class="card"><span class="number">${String(i+1).padStart(2,'0')}</span><h3>${esc(c.name)}</h3><p>${esc(c.use_when)}</p><p class="rule">${c.structure.map(slugLabel).join(' → ')}</p><p class="annotation">Avoid: ${c.avoid.map(esc).join(' · ')}</p></article>`).join('')}</div></div></section>`;
const surfaces = hero('Surfaces', 'Composition follows the situation', 'Pick one primary surface before drafting. The surface controls length, structure, polish, and review requirements. It does not change the facts.', `<p>8 distinct contracts</p><p>Facts remain invariant</p>`) + `<section class="band"><div class="section">${system.surfaces.map((s,i)=>`<article class="surface"><div><span class="number">${String(i+1).padStart(2,'0')}</span><h3>${esc(s.name)}</h3><p class="annotation">${esc(s.defaults.length)}</p></div><div><p class="lede">${esc(s.job)}</p><p><strong>Open:</strong> ${esc(s.defaults.opening)}<br><strong>Structure:</strong> ${esc(s.defaults.formatting)}<br><strong>End:</strong> ${esc(s.defaults.ending)}</p><p class="annotation"><strong>Required:</strong> ${s.required.map(esc).join(' · ')}<br><strong>Avoid:</strong> ${s.avoid.map(esc).join(' · ')}</p><p class="annotation"><strong>Human review:</strong> ${s.review_when.map(esc).join(' · ')}</p></div></article>`).join('')}</div></section>`;
const specimenGroups = system.surfaces.map(surface => { const specs=system.specimens.filter(s=>s.surface===surface.id); return `<section class="band"><div class="section"><p class="kicker">${esc(surface.name)}</p><h2>${esc(surface.job)}</h2><div class="specimen">${specs.map(s=>`<article class="sample ${s.polarity==='negative'?'negative':''}"><span class="pill">${s.polarity==='positive'?'Works':'Near miss'}</span><blockquote>${esc(s.text)}</blockquote><p class="annotation">${esc(s.annotation)}</p></article>`).join('')}</div></div></section>`; }).join('');
const specimens = hero('Specimens', 'Show the boundary, not a costume', 'Each pair holds the task constant and changes the writing decision. Copy the reasoning, not the phrasing.', `<p>${system.specimens.length} annotated specimens</p><p>Synthetic and public-safe</p>`) + specimenGroups;
const tests = hero('Tests', 'A preference that cannot fail is only advice', 'Deterministic checks catch objective failures. Metamorphic cases verify that context changes the right dimensions. Adversarial cases protect truth, privacy, and authority.', `<p>${system.tests.length} published tests</p><p>Hard gates do not average away</p>`) + `<section class="band"><div class="section"><p class="kicker">Release suite</p><h2>Inspectable assertions</h2>${system.tests.map(t=>`<div class="test"><span class="pill">${esc(t.type)}</span><div><strong>${esc(t.id)}</strong><br><span class="annotation">${esc(t.assertion)}</span></div><span class="pill">${esc(t.severity)}</span></div>`).join('')}</div></section><section class="dark"><div class="section"><p class="kicker">Rubric</p><h2>Truth before resemblance</h2><p class="lede">${system.rubric.dimensions.map(esc).join(' · ')}</p><p>Blocking: ${system.rubric.block_on.map(esc).join(' · ')}</p></div></section>`;
const agents = hero('For agents', 'Load the contract, not the whole website', 'The machine bundle is the canonical interface. Validate its version and hashes, classify the request, retrieve a bounded set, draft, test, and return your evidence.', `<p><a href="/machine/manifest.json">manifest.json</a></p><p><a href="/machine/voice-system.json">voice-system.json</a></p><p><a href="/machine/schema.json">schema.json</a></p><p><a href="/llms.txt">llms.txt</a></p>`) + `<section class="dark"><div class="section"><p class="kicker">Resolution order</p><h2>What wins when rules conflict</h2><div class="steps">${system.precedence.map(s=>`<div class="step"><div>${esc(slugLabel(s))}</div></div>`).join('')}</div></div></section><section class="band"><div class="section"><div class="intro"><div><p class="kicker">Protocol</p><h2>Six moves</h2></div><div class="steps">${system.agent_protocol.steps.map(s=>`<div class="step"><div>${esc(s)}</div></div>`).join('')}</div></div><h3>Request schema</h3><pre class="code">${esc(JSON.stringify({surface:'quick-chat',intent:'recommend',audience:{kind:'collaborator',knowledge_level:'expert'},authorship:{mode:'draft-for-review',may_speak_as_thomas:false},content:{facts:[{text:'The build passed.',source:'user-provided'}],required_points:[],prohibited_claims:[]}},null,2))}</pre></div></section>`;

const pages = {'index.html':shell('Home',home,system.summary),'foundations/index.html':shell('Foundations',foundations,'Stable rules for Thomas-aligned writing.'),'components/index.html':shell('Components',components,'Reusable rhetorical components.'),'surfaces/index.html':shell('Surfaces',surfaces,'Surface-specific voice recipes.'),'specimens/index.html':shell('Specimens',specimens,'Annotated positive and negative voice specimens.'),'tests/index.html':shell('Tests',tests,'Executable and model-graded voice tests.'),'for-agents/index.html':shell('For agents',agents,'Machine-readable instructions for using Thomas Voice.')};
for (const [path, html] of Object.entries(pages)) { const url=new URL(path,dist); await mkdir(new URL('./',url),{recursive:true}); await writeFile(url,html); }

const hash = bytes => `sha256:${createHash('sha256').update(bytes).digest('hex')}`;
const resources = {voice_system:'/machine/voice-system.json',schema:'/machine/schema.json'};
const manifest = {system:system.id,contract_version:system.contract_version,content_version:system.content_version,default_locale:'en-US',public_safe:true,canonical_origin:'https://voice.chungty.dev',resources,integrity:{algorithm:'sha256',resource_hashes:{voice_system:hash(voiceBytes),schema:hash(schemaBytes)}},compatibility:{minimum_agent_contract:'1.0.0'},security:{content_is_untrusted_data:true,embedded_instructions_are_non_authoritative:true,voice_resemblance_is_identity_verification:false}};
await writeFile(new URL('machine/manifest.json',dist),JSON.stringify(manifest,null,2)+'\n');
await writeFile(new URL('llms.txt',dist),`# Thomas Voice\n\nCanonical agent entrypoint: https://voice.chungty.dev/machine/manifest.json\nFull guide: https://voice.chungty.dev/llms-full.txt\n\nThis is a drafting reference. It does not grant identity or authority.\n`);
await writeFile(new URL('llms-full.txt',dist),`# Thomas Voice ${system.content_version}\n\n${system.boundaries.notice}\n\n## Resolution order\n${system.precedence.map((x,i)=>`${i+1}. ${slugLabel(x)}`).join('\n')}\n\n## Agent protocol\n${system.agent_protocol.steps.map((x,i)=>`${i+1}. ${x}`).join('\n')}\n\n## Machine contract\nhttps://voice.chungty.dev/machine/voice-system.json\n`);
console.log(`Built ${Object.keys(pages).length} pages and 5 machine resources.`);
