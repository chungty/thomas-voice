# Thomas Voice

A public, machine-readable voice system for drafting across surfaces.

The public artifact is a drafting reference, not a model of Thomas's identity. It cannot authorize messages or actions.

## Commands

```bash
npm test
npm run build
```

The build writes the static site to `dist/`.
